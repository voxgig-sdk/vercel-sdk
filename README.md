# Vercel SDK

Vercel API client, generated from the OpenAPI spec.

Vercel combines the best developer experience with an obsessive focus on end-user performance.

Learn more about Vercel API at [vercel.com](https://vercel.com).

This is an unofficial SDK for the Vercel public API, generated by Voxgig with [`@voxgig/sdkgen`](https://github.com/voxgig/sdkgen). It is not affiliated with, endorsed by, or sponsored by the upstream API provider.

Learn more about Voxgig SDKs at [voxgig.com/sdk](https://voxgig.com/sdk/).

Full documentation for this SDK: [https://voxgig-sdk.github.io/vercel-sdk/](https://voxgig-sdk.github.io/vercel-sdk/)

> TypeScript, Python, PHP, Golang, Lua, JavaScript SDKs, a CLI with an interactive REPL, and an MCP server for AI agents — all generated from one OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).

> **Features:** `debug`, `idempotency`, `metrics`, `paging`, `ratelimit`, `retry`, `test`, `timeout` — opt-in,
> inactive until switched on, and configured per client. See the Features
> section of any SDK README below for what each one does.

## Entities, not endpoints

This SDK exposes the API as **69 semantic entities** that you
call directly, instead of assembling URL paths and query strings. See the [Entities](#entities) table below for the full list. Entities are
**Capitalised** to mark them as the primary surface, each with the operations they
support (`list`, `load`, `create`, `update`, `remove`, `patch`):

```ts
const client = new VercelSDK()
const items = await client.AccessGroup().list({ id_or_name: "example" })
```

Thinking in entities keeps the mental model small — for people and AI agents alike —
rather than reasoning about raw HTTP routes and query parameters.

## Offline unit testing

Every SDK ships a built-in **test mode** that swaps the HTTP transport for
an in-memory mock, so your unit tests run fully offline — no server, no
network, and no credentials:

### TypeScript

```ts
// The offline mock starts EMPTY — seed it with the records the test needs.
// Shape: { entity: { <entity-name>: { <id>: <record> } } }
const client = VercelSDK.test({
  entity: {
    bulk_redirect: {
      test01: { id: 'test01', project_id: 'example_project_id', createdBy: 'example_createdBy' },
    },
  },
})
const bulkredirects = await client.BulkRedirect().list()
// bulkredirects is an array of BulkRedirect entities, populated with mock data
// — call bulkredirects[0].data() for the record itself
console.log(bulkredirects)
```

### Python

```python
client = VercelSDK.test()
bulkredirects = client.BulkRedirect().list()
print(bulkredirects)
```

### PHP

```php
// Seed fixture data so offline calls resolve without a live server.
$client = VercelSDK::test([
    "entity" => ["bulkredirect" => ["test01" => []]],
]);
$bulkredirects = $client->BulkRedirect()->list();
```

### Golang

```go
client := sdk.Test()
result, err := client.BulkRedirect(nil).List(
    nil, nil,
)
```

### Lua

```lua
local client = sdk.test()
local results, err = client:BulkRedirect():list()
```

### JavaScript

```js
const client = VercelSDK.test()
const bulkredirects = await client.BulkRedirect().list()
// bulkredirects is an array of entities, populated with mock data
// — call bulkredirects[0].data() for the record itself
console.log(bulkredirects)
```

## Packages

| Language | Package | Install |
| --- | --- | --- |
| TypeScript | `@voxgig-sdk/vercel` | publish pending — [install from git tag](https://github.com/voxgig-sdk/vercel-sdk/tags) |
| Python | `voxgig-sdk-vercel` | publish pending — [install from git tag](https://github.com/voxgig-sdk/vercel-sdk/tags) |
| PHP | `voxgig-sdk/vercel` | publish pending — [install from git tag](https://github.com/voxgig-sdk/vercel-sdk/tags) |
| Golang | `github.com/voxgig-sdk/vercel-sdk/go` | `go get github.com/voxgig-sdk/vercel-sdk/go@latest` |
| Lua | `voxgig-sdk-vercel` | publish pending — [install from git tag](https://github.com/voxgig-sdk/vercel-sdk/tags) |
| JavaScript | `@voxgig-sdk/vercel-js` | publish pending — [install from git tag](https://github.com/voxgig-sdk/vercel-sdk/tags) |
| Go CLI | `github.com/voxgig-sdk/vercel-sdk/go-cli` | `go install github.com/voxgig-sdk/vercel-sdk/go-cli/cmd/vercel@latest` |
| Go MCP server | `github.com/voxgig-sdk/vercel-sdk/go-mcp` | `go get github.com/voxgig-sdk/vercel-sdk/go-mcp@latest` |

## Quickstart

### TypeScript

```ts
import { VercelSDK } from '@voxgig-sdk/vercel'

const client = new VercelSDK({
  apikey: process.env.VERCEL_APIKEY,
})

// List all accessgroups (returns AccessGroupEntity[] — .data() for the record)
const accessgroups = await client.AccessGroup().list({ id_or_name: "example" })
for (const accessgroup of accessgroups) {
  console.log(accessgroup)
}

// Load a specific authentication (returns a Authentication)
const authentication = await client.Authentication().load({
  token_id: 'example_token_id',
})
console.log(authentication)
```

See the [TypeScript README](ts/README.md) for the full guide.

## Surfaces

| Surface | Path |
| --- | --- |
| **SDK** (TypeScript, Python, PHP, Golang, Lua, JavaScript) | `ts/` `py/` `php/` `go/` `lua/` `js/` |
| **CLI** | `go-cli/` |
| **MCP server** | `go-mcp/` |

## Use it from an AI agent (MCP)

The generated MCP server exposes every operation in this SDK as an
[MCP](https://modelcontextprotocol.io) tool that Claude, Cursor or Cline
can call directly. Build and register it:

```bash
cd go-mcp && go build -o vercel-mcp .
```

Then add it to your agent's MCP config (Claude Desktop, Cursor, etc.):

```json
{
  "mcpServers": {
    "vercel": {
      "command": "/abs/path/to/vercel-mcp"
    }
  }
}
```

## Entities

The API exposes 69 entities:

| Entity | Description | API path |
| --- | --- | --- |
| **AccessGroup** | The AccessGroup entity (create, list, load, remove, update). | `/v1/access-groups/{idOrName}/members` |
| **AiGateway** | The AiGateway entity (remove). | `/v1/ai-gateway/rules` |
| **AiGatewayRule** | The AiGatewayRule entity (create, update). | `/v1/ai-gateway/rules` |
| **AiGatewayRuleList** | The AiGatewayRuleList entity (list). | `/v1/ai-gateway/rules` |
| **AiGatewayVirtualModelConfig** | The AiGatewayVirtualModelConfig entity (create, load, update). | `/v1/ai-gateway/virtual-model-configs/{vmcSlug}` |
| **AiGatewayVirtualModelConfigList** | The AiGatewayVirtualModelConfigList entity (list). | `/v1/ai-gateway/virtual-model-configs/list` |
| **Alias** | The Alias entity (create, list, load, remove, update). | `/v4/aliases` |
| **ApiAiGateway** | The ApiAiGateway entity (load, remove). | `/v1/ai-gateway/virtual-model-configs` |
| **ApiKey** | The ApiKey entity (create). | `/api-keys` |
| **Artifact** | The Artifact entity (create, load, remove, update). | `/v8/artifacts/{hash}` |
| **Authentication** | The Authentication entity (create, load, remove). | `/v5/user/tokens/{tokenId}` |
| **Billing** | The Billing entity (create, load). | `/v1/billing/charges` |
| **BulkRedirect** | The BulkRedirect entity (create, list, load, patch, remove, update). | `/v1/bulk-redirects/versions` |
| **Cert** | The Cert entity (create, list, load, remove, update). | `/v8/certs` |
| **Check** | The Check entity (create, list, load, remove, update). | `/v2/projects/{projectIdOrName}/checks` |
| **ChecksV2** | The ChecksV2 entity (create, list, load, update). | `/v2/projects/{projectIdOrName}/checks/{checkId}/runs` |
| **Connect** | The Connect entity (create, remove). | `/v1/connect/authorize/{connector}` |
| **ConnectConnector** | The ConnectConnector entity (create, load, update). | `/v1/connect/connectors/{connector}` |
| **ConnectConnectorList** | The ConnectConnectorList entity (list). | `/v2/connect/connectors` |
| **ConnectConnectorProjectConnectionList** | The ConnectConnectorProjectConnectionList entity (list). | `/v2/connect/connectors/{connector}/projects` |
| **ConnectProjectConnection** | The ConnectProjectConnection entity (create, load). | `/v1/connect/connectors/{connector}/projects/{projectId}` |
| **ConnectProjectConnectorConnectionList** | The ConnectProjectConnectorConnectionList entity (list). | `/v2/connect/projects/{projectId}/connectors` |
| **Deployment** | The Deployment entity (create, list, load, remove, update). | `/v7/deployments` |
| **Dns** | The Dns entity (create, load, remove, update). | `/v5/domains/{domain}/records` |
| **Domain** | The Domain entity (create, list, load, remove, update). | `/v5/domains` |
| **DomainsRegistrar** | The DomainsRegistrar entity (create, load, update). | `/v1/registrar/domains/{domain}/price` |
| **Drain** | The Drain entity (create, load, remove, update). | `/v1/drains` |
| **EdgeCache** | The EdgeCache entity (create). | `/v1/edge-cache/dangerously-delete-by-src-images` |
| **Env** | The Env entity (create, list, load, remove, update). | `/v1/env` |
| **Environment** | The Environment entity (create, list, load, remove, update). | `/v9/projects/{idOrName}/custom-environments` |
| **FeatureFlag** | The FeatureFlag entity (list, load, patch, remove, update). | `/v2/teams/{teamId}/feature-flags/flags` |
| **File** | The File entity (list). | `/v6/deployments/{id}/files` |
| **Flag** | The Flag entity (load). | `/v1/projects/{projectIdOrName}/feature-flags/flags/{flagIdOrSlug}` |
| **FlagsSdkKeyWithSecret** | The FlagsSdkKeyWithSecret entity (update). | `/v1/projects/{projectIdOrName}/feature-flags/sdk-keys` |
| **GlobalConfig** | The GlobalConfig entity (create, list, load, patch, remove, update). | `/v1/global-config/{edgeConfigId}/backups` |
| **GlobalConfigItem** | The GlobalConfigItem entity (list, load). | `/v1/global-config/{edgeConfigId}/items` |
| **GlobalConfigToken** | The GlobalConfigToken entity (load). | `/v1/global-config/{edgeConfigId}/token/{token}` |
| **Integration** | The Integration entity (create, list, load, remove). | `/v1/integrations/integration/{integrationIdOrSlug}/products/{productIdOrSlug}/plans` |
| **Kms** | The Kms entity (create, list, load, remove, update). | `/v1/kms/issuers` |
| **ListEventType** | The ListEventType entity (list). | `/v1/events/types` |
| **Log** | The Log entity (load). | `/v1/projects/{projectId}/deployments/{deploymentId}/runtime-logs` |
| **LogDrain** | The LogDrain entity (create, list, load, remove). | `/v2/integrations/log-drains` |
| **Marketplace** | The Marketplace entity (create, list, load, patch, remove, update). | `/v1/installations/{integrationConfigurationId}/resources` |
| **Microfrontend** | The Microfrontend entity (create, list, load). | `/v1/microfrontends/groups/{groupId}/projects` |
| **Network** | The Network entity (create, list, load, remove, update). | `/v1/connect/networks` |
| **Networking** | The Networking entity (remove, update). | `/v1/projects/{idOrName}/shared-connect-links` |
| **Observability** | The Observability entity (list, update). | `/v1/observability/manage/configuration/projects` |
| **PrivateLinkEndpoint** | The PrivateLinkEndpoint entity (create, list, load, update). | `/v1/networking/privatelink/endpoints` |
| **Project** | The Project entity (create, load, patch, remove, update). | `/v10/projects` |
| **ProjectMember** | The ProjectMember entity (create, load, remove). | `/v1/projects/{idOrName}/members` |
| **ProjectRoute** | The ProjectRoute entity (create, list, load, patch, remove, update). | `/v1/projects/{projectId}/routes/versions` |
| **Query** | The Query entity (create). | `/v2/observability/query` |
| **Record** | The Record entity (load). | `/domains/records/{recordId}` |
| **RollingRelease** | The RollingRelease entity (create, load, remove, update). | `/v1/projects/{idOrName}/rolling-release` |
| **Sandbox** | The Sandbox entity (create, list, load, remove, update). | `/v2/sandboxes` |
| **Schema** | The Schema entity (list, load). | `/v2/observability/schema` |
| **Security** | The Security entity (create, list, load, patch, remove, update). | `/v1/security/firewall/bypass` |
| **Segment** | The Segment entity (load). | `/v1/projects/{projectIdOrName}/feature-flags/segments/{segmentIdOrSlug}` |
| **Storage** | The Storage entity (create, load, remove). | `/storage/stores/{id}` |
| **Team** | The Team entity (create, list, load, remove, update). | `/v2/teams` |
| **TldName** | The TldName entity (list). | `/v1/registrar/tlds/supported` |
| **Toggle** | The Toggle entity (create). | `/speed-insights/toggle` |
| **User** | The User entity (list, load, remove). | `/v3/events` |
| **Vcr** | The Vcr entity (create, list, load, patch, remove, update). | `/v1/vcr/repository/{idOrName}/tags` |
| **VcrImageList** | The VcrImageList entity (list). | `/v1/vcr/repository/{idOrName}/images` |
| **VcrRepositoryList** | The VcrRepositoryList entity (list). | `/v1/vcr/repository` |
| **VcrRepositoryPermissionList** | The VcrRepositoryPermissionList entity (list). | `/v1/vcr/repository/{idOrName}/permissions` |
| **WebAnalytics** | The WebAnalytics entity (load). | `/v1/query/web-analytics/events/aggregate` |
| **Webhook** | The Webhook entity (create, load, remove). | `/v1/webhooks/{id}` |

The operations available across these entities are **load**, **list**, **create**, **update**, **remove** — see each entity's
own list above for exactly which it supports.

## Quickstart in other languages

### Python

```python
import os
from vercel_sdk import VercelSDK

client = VercelSDK({
    "apikey": os.environ.get("VERCEL_APIKEY"),
})

# List all accessgroups (returns a list, raises on error)
accessgroups = client.AccessGroup().list({"id_or_name": "example"})
for accessgroup in accessgroups:
    print(accessgroup)

# Load a specific accessgroup (returns the record, raises on error)
accessgroup = client.AccessGroup().load({"id": "example_id"})
print(accessgroup)
```

### PHP

```php
<?php
require_once 'vercel_sdk.php';

$client = new VercelSDK([
    "apikey" => getenv("VERCEL_APIKEY"),
]);

// List all accessgroups (returns an array; throws on error)
$accessgroups = $client->AccessGroup()->list();
print_r(array_map(fn($item) => $item->data_get(), $accessgroups));

// Load a specific accessgroup (returns the ENTITY; call data_get() for the record; throws on error)
$accessgroup = $client->AccessGroup()->load(["id" => "example_id"]);
print_r($accessgroup->data_get());
```

### Golang

```go
import sdk "github.com/voxgig-sdk/vercel-sdk/go"

client := sdk.NewVercelSDK(map[string]any{
    "apikey": os.Getenv("VERCEL_APIKEY"),
})

// List all accessgroups
accessGroups, err := client.AccessGroup(nil).List(nil, nil)
if err != nil {
    panic(err)
}
fmt.Println(accessGroups)

// Load a specific authentication
authentication, err := client.Authentication(nil).Load(
    map[string]any{"token_id": "example_token_id"}, nil,
)
if err != nil {
    panic(err)
}
fmt.Println(authentication)
```

### Lua

```lua
local sdk = require("vercel_sdk")

local client = sdk.new({
  apikey = os.getenv("VERCEL_APIKEY"),
})

-- List all accessgroups
local accessgroups, err = client:AccessGroup():list()
print(accessgroups)

-- Load a specific accessgroup
local accessgroup, err = client:AccessGroup():load({ id = "example_id" })
print(accessgroup)
```

### JavaScript

```js
const { VercelSDK } = require('@voxgig-sdk/vercel-js')

const client = new VercelSDK({
  apikey: process.env.VERCEL_APIKEY,
})

// List all accessgroups (returns an array)
const accessgroups = await client.AccessGroup().list({ id_or_name: "example" })
for (const accessgroup of accessgroups) {
  console.log(accessgroup)
}

// Load a specific authentication (returns the entity)
const authentication = await client.Authentication().load({
  token_id: 'example_token_id',
})
console.log(authentication)
```

## Direct and prepare

For endpoints the entity model doesn't cover, use the low-level methods:

- **`direct(fetchargs)`** — build and send an HTTP request in one step.
- **`prepare(fetchargs)`** — build the request without sending it.

Both accept a map with `path`, `method`, `params`, `query`,
`headers`, and `body`. See the [How-to guides](#how-to-guides) below.

## How-to guides

### Make a direct API call

When the entity interface does not cover an endpoint, use `direct`:

**TypeScript:**
```ts
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

**Python:**
```python
result = client.direct({
    "path": "/api/resource/{id}",
    "method": "GET",
    "params": {"id": "example"},
})
```

**PHP:**
```php
$result = $client->direct([
    "path" => "/api/resource/{id}",
    "method" => "GET",
    "params" => ["id" => "example"],
]);
```

**Go:**
```go
result, err := client.Direct(map[string]any{
    "path":   "/api/resource/{id}",
    "method": "GET",
    "params": map[string]any{"id": "example"},
})
if err != nil {
    panic(err)
}
fmt.Println(result)
```

**Lua:**
```lua
local result, err = client:direct({
  path = "/api/resource/{id}",
  method = "GET",
  params = { id = "example" },
})
```

**JavaScript:**
```js
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

## Advanced

> Everyday use only needs the sections above. This explains the internals
> behind every call — relevant when writing custom features.

Every SDK call runs the same five-stage pipeline:

1. **Point** — resolve the API endpoint from the operation definition.
2. **Spec** — build the HTTP specification (URL, method, headers, body).
3. **Request** — send the HTTP request.
4. **Response** — receive and parse the response.
5. **Result** — extract the result data for the caller.

A feature hook fires at each stage (e.g. `PrePoint`, `PreSpec`,
`PreRequest`), so features can inspect or modify the pipeline without
forking the SDK.

### Features

| Feature | Purpose |
| --- | --- |
| **DebugFeature** | Request/response capture ring buffer for debugging |
| **IdempotencyFeature** | Idempotency keys for safe retries of mutating operations |
| **MetricsFeature** | Statistics capture: per-operation counters and latency |
| **PagingFeature** | Pagination signals for list operations |
| **RatelimitFeature** | Client-side rate limiting via a token bucket |
| **RetryFeature** | Automatic retry of transient failures with exponential backoff |
| **TestFeature** | In-memory mock transport for testing without a live server |
| **TimeoutFeature** | Per-request timeout with transport abort |

Pass custom features via the `extend` option at construction time.

## Customizing this SDK

This repository contains its own generator (`.sdk/`), so the SDK is
customizable without forking any upstream tool:

- **The model** (`.sdk/model/`) declares everything this project owns:
  package names, versions, active features, per-target settings. It is
  written in [aontu](https://aontu.dev), a JSON-based
  specification language designed for building ontologies: easy to edit
  by hand, and files unify rather than override, so small declarations
  compose into one model. Regeneration re-reads it every time.
- **Templates** (`.sdk/tm/`) and **components** (`.sdk/src/cmp/`) are
  the two layers of generation, copied into this repo: templates are the
  literal per-language source, components generate the API-shaped parts.
- **Regeneration merges.** By default, newly generated content is
  three-way merged into existing files, so generator updates and local
  edits usually converge without manual conflict handling. A project can
  opt for plain overwrite instead.
- **Custom features and entire custom targets** arrive through sdkgen
  packages (`voxgig-sdkgen package add`), on the same rails as the
  bundled languages, and `voxgig-sdkgen doctor` reports any drift from
  what a resync would write.

How-to: [customize and propagate templates](https://github.com/voxgig/sdkgen/blob/main/docs/how-to/customize-and-propagate-templates.md).
The full story: [voxgig.com/sdk/custom](https://voxgig.com/sdk/custom).

## Per-language documentation

- [TypeScript](ts/README.md)
- [Python](py/README.md)
- [PHP](php/README.md)
- [Golang](go/README.md)
- [Lua](lua/README.md)
- [JavaScript](js/README.md)

## Upstream API

This SDK is generated from the upstream OpenAPI specification. It is an
unofficial client and is not affiliated with the API provider.

The OpenAPI spec(s) this SDK was generated from are kept in the
[`.sdk/def/`](.sdk/def/) folder.

- Upstream API: [https://vercel.com/support](https://vercel.com/support)

## Security

Please report security issues to security@voxgig.com. See [SECURITY.md](SECURITY.md).
Do not open public issues for suspected vulnerabilities.

---

Generated from the Vercel API OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).
