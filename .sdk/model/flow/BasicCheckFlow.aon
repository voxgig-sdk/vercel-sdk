# BasicCheckFlow

main: kit: flow: BasicCheckFlow:
{
  entity: "check",
  kind: "basic",
  name: "BasicCheckFlow",
  step: [
    {
      input: {
        ref: "check_ref01"
      }

      match: {
        deployment_id: "deployment01",
        project_id: "project01",
        project_id_or_name: "project_or_name01"
      }

      op: "create"
    }

    {
      match: {
        deployment_id: "deployment01"
      }

      op: "list",
      valid: [
        {
          apply: "ItemExists",
          def: {
            ref: "check_ref01"
          }
        }
      ]
    }

    {
      data: {
        project_id: "project01"
      }

      input: {
        ref: "check_ref01",
        srcdatavar: "check_ref01_data",
        suffix: "_up0",
        textfield: "blocks"
      }

      op: "update",
      spec: [
        {
          apply: "TextFieldMark",
          def: {
            mark: "Mark01-check_ref01"
          }
        }
      ]
    }

    {
      input: {
        ref: "check_ref01",
        srcdatavar: "check_ref01_data",
        suffix: "_dt0"
      }

      match: {
        id: "check01",
        project_id: "project01"
      }

      op: "load",
      valid: [
        {
          apply: "TextFieldMark",
          def: {
            mark: "Mark01-check_ref01"
          }
        }
      ]
    }

    {
      input: {
        ref: "check_ref01",
        suffix: "_rm0"
      }

      match: {
        id: "check01",
        project_id: "project01"
      }

      op: "remove"
    }

    {
      input: {
        suffix: "_rt0"
      }

      match: {
        deployment_id: "deployment01"
      }

      op: "list",
      valid: [
        {
          apply: "ItemNotExists",
          def: {
            ref: "check_ref01"
          }
        }
      ]
    }
  ],
  "key$": "BasicCheckFlow"
}