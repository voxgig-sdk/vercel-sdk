# BasicApiKeyFlow

main: kit: flow: BasicApiKeyFlow:
{
  entity: "api_key",
  kind: "basic",
  name: "BasicApiKeyFlow",
  step: [
    {
      input: {
        ref: "api_key_ref01"
      }

      op: "create"
    }
  ],
  "key$": "BasicApiKeyFlow"
}